# hfs-wake-on-lan
Wake-on-LAN dashboard for HFS. Wake and monitor network devices via magic packets. Features ICMP ping (+ optional TCP port badge), persistent device storage in plugin config, add/remove with 8-second undo, and a responsive mobile-first UI.
